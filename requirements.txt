# CVAS dependency manifest.
#
# Fast analysis mode is intentionally lightweight. The workspace test
# environment provides pycparser; CVAS also has text fallbacks for partial
# analysis when AST parsing is unavailable.
#
# Install this file with pip to get the development/test dependency.

pytest

# Optional full-mode structural backend, install manually when desired:
# tree_sitter
# tree_sitter_c
# tree_sitter_cpp
#
# Full mode uses the system gcc/g++ executables for GCC dump metadata and does
# not require Python clang bindings or libclang.
