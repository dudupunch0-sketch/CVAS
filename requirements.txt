# CVAS Python dependency manifest.
#
# Runtime:
# - pycparser is used by the default fast analysis path.
# - tree-sitter plus C/C++ grammars are used by full-mode structural discovery.
#
# Test/dev:
# - pytest runs the regression and contract suite.
# - jsonschema validates checked-in schema examples.
#
# System tools:
# - full mode also uses system gcc/g++ for GCC dump metadata.
# - Python clang bindings and libclang are not required for the public full path.

pycparser
tree-sitter
tree-sitter-c
tree-sitter-cpp
pytest
jsonschema
