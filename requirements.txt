# CVAS base runtime.
# Fast analysis mode uses only the Python standard library, so there are no
# required pip packages for the default runtime.
#
# Installation guide for an automated agent:
# - To run tests or develop locally, install: pip install -r requirements-dev.txt
# - To use --analysis-mode full, install: pip install -r requirements-full.txt
