name: CVAS Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

# jobs:
#   test:
#     name: Test on Python ${{ matrix.python-version }}
#     runs-on: ubuntu-latest

#     strategy:
#       fail-fast: false
#       matrix:
#         python-version: ["3.10", "3.11", "3.12"]

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Python ${{ matrix.python-version }}
#         uses: actions/setup-python@v5
#         with:
#           python-version: ${{ matrix.python-version }}

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           # Pin test toolchain for deterministic regression snapshots.
#           pip install pytest==9.0.2 pycparser==2.22

#       - name: Run regression tests
#         run: |
#           pytest tests/test_regression.py -v --tb=short

#       - name: Check for snapshot drift
#         run: |
#           # Ensure no .expected.json files were modified
#           if git diff --exit-code -- ':(glob)tests/fixtures/**/*.expected.json'; then
#             echo "✓ No snapshot drift detected"
#           else
#             echo "✗ Snapshot files changed!"
#             echo "If this is intentional, commit the changes."
#             echo "If not, snapshots may have regressed."
#             git diff -- ':(glob)tests/fixtures/**/*.expected.json'
#             exit 1
#           fi

#       - name: Upload test results
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: test-results-py${{ matrix.python-version }}
#           path: |
#             tests/
#             !tests/__pycache__/
#           retention-days: 30

  lint:
    name: Lint Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install linting tools
        run: |
          pip install black flake8

      - name: Run black (code formatting check)
        run: |
          black --check src/ tests/ || echo "Run 'black src/ tests/' to auto-format"

      - name: Run flake8 (style check)
        run: |
          flake8 src/ tests/ --max-line-length=120 --extend-ignore=E203,W503

#   coverage:
#     name: Test Coverage
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"

#       - name: Install dependencies
#         run: |
#           pip install pytest pytest-cov pycparser

#       - name: Run tests with coverage
#         run: |
#           pytest tests/ --cov=src --cov-report=term --cov-report=html

#       - name: Upload coverage report
#         uses: actions/upload-artifact@v4
#         with:
#           name: coverage-report
#           path: htmlcov/
#           retention-days: 30
